# override any codebloom properties here

codebloom:
  scheduling:
    enabled: false
  notify:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        # add beans and env
        include: prometheus, health, beans, env
