apiVersion: v1
kind: Service
metadata:
  name: gerrit
  namespace: infrastructure
spec:
  selector:
    app: gerrit
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
