apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: infrastructure-headscale-tls
  namespace: infrastructure
spec:
  secretName: infrastructure-headscale-tls
  dnsNames:
    - headscale.patinanetwork.org
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
