apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: proxy-traefik-tls
  namespace: infrastructure
spec:
  secretName: proxy-traefik-tls
  dnsNames:
    - "*.proxy.patinanetwork.org"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
