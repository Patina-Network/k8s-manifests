apiVersion: v1
kind: Service
metadata:
  name: headscale
  namespace: infrastructure
spec:
  selector:
    app: headscale
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
