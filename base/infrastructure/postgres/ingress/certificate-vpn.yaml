apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: postgres-vpn-tls
  namespace: infrastructure
spec:
  secretName: postgres-vpn-tls
  dnsNames:
    - db.vpn.patinanetwork.org
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
