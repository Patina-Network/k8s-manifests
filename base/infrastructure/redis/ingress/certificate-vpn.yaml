apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: redis-vpn-tls
  namespace: infrastructure
spec:
  secretName: redis-vpn-tls
  dnsNames:
    - redis.vpn.patinanetwork.org
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
