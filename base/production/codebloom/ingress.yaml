apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: codebloom-web
  namespace: production
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`codebloom.patinanetwork.org`) && PathPrefix(`/`)
      kind: Rule
      middlewares:
        - name: redirect-to-https
      services:
        - name: codebloom
          port: 80
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: codebloom-websecure
  namespace: production
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`codebloom.patinanetwork.org`) && (Path(`/actuator/health/liveness`) || Path(`/actuator/health/readiness`))
      kind: Rule
      priority: 10
      services:
        - name: codebloom
          port: 80
    - match: Host(`codebloom.patinanetwork.org`) && PathPrefix(`/actuator`)
      kind: Rule
      priority: 5
      middlewares:
        - name: basic-auth
      services:
        - name: codebloom
          port: 80
    - match: Host(`codebloom.patinanetwork.org`) && PathPrefix(`/`)
      kind: Rule
      priority: 1
      middlewares:
        - name: basic-rate-limit
      services:
        - name: codebloom
          port: 80
  tls:
    secretName: production-codebloom-tls
