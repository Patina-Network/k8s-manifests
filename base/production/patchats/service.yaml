apiVersion: v1
kind: Service
metadata:
  name: patchats
  namespace: production
spec:
  selector:
    app: patchats
  ports:
    - name: http-web
      protocol: TCP
      port: 80
      targetPort: 8080
