apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: staging-codebloom-proxy-tls
  namespace: staging
spec:
  secretName: staging-codebloom-proxy-tls
  dnsNames:
    - stg.codebloom.proxy.patinanetwork.org
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
