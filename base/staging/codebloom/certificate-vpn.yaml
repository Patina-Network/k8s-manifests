apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: staging-codebloom-vpn-tls
  namespace: staging
spec:
  secretName: staging-codebloom-vpn-tls
  dnsNames:
    - stg.codebloom.vpn.patinanetwork.org
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
