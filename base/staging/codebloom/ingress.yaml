apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: codebloom-web
  namespace: staging
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`stg.codebloom.patinanetwork.org`) && PathPrefix(`/`)
      kind: Rule
      middlewares:
        - name: redirect-to-https
      services:
        - name: codebloom
          port: 80
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: codebloom-websecure
  namespace: staging
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`stg.codebloom.patinanetwork.org`) && (Path(`/actuator/health/liveness`) || Path(`/actuator/health/readiness`))
      kind: Rule
      priority: 10
      services:
        - name: codebloom
          port: 80
    - match: Host(`stg.codebloom.patinanetwork.org`) && PathPrefix(`/actuator`)
      kind: Rule
      priority: 5
      middlewares:
        - name: basic-auth
      services:
        - name: codebloom
          port: 80
    - match: Host(`stg.codebloom.patinanetwork.org`) && PathPrefix(`/`)
      kind: Rule
      middlewares:
        - name: basic-rate-limit
      priority: 1
      services:
        - name: codebloom
          port: 80
  tls:
    secretName: staging-codebloom-tls
