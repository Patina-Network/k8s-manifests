apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: headscale-authkey-infrastructure
  namespace: flux-system
spec:
  interval: 10m
  path: ./base/infrastructure/headscale/authkey
  dependsOn:
    - name: headscale-infrastructure
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: infrastructure
  decryption:
    provider: sops
