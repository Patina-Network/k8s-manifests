apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: headscale-proxy-traefik-infrastructure
  namespace: flux-system
spec:
  interval: 10m
  path: ./base/infrastructure/headscale/proxy-traefik
  dependsOn:
    - name: traefik-infrastructure
    - name: headscale-authkey-infrastructure
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: infrastructure
